# Base version for the project
# This version is used for tagged releases and as the base for snapshot builds
# Update this when preparing a new release
version=1.0.0

# Gradle settings
org.gradle.jvmargs=-Xmx2g
org.gradle.parallel=true
org.gradle.caching=true
